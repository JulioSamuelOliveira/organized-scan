spring.application.name=organized-scan

# Postgres local (ajuste conforme seu compose)
spring.datasource.url=jdbc:postgresql://127.0.0.1:5432/organizedscan
spring.datasource.username=organizedscan
spring.datasource.password=organizedscan

# JPA/Flyway
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true

spring.flyway.enabled=true
spring.flyway.locations=classpath:db

# Thymeleaf: enable hidden _method (DELETE/PUT)
spring.mvc.hiddenmethod.filter.enabled=true

# OAuth (quando for testar de novo)
spring.security.oauth2.client.registration.github.client-id=Ov23liTzKRI5A47nomHv
spring.security.oauth2.client.registration.github.client-secret=c42a35e12d0f0d6615d6e87d981a4c1c2bc146fe
spring.security.oauth2.client.registration.github.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}
